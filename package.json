{
  "name": "etsy-orchestrator",
  "private": true,
  "workspaces": ["shared", "agents", "backend", "frontend"],
  "scripts": {
    "dev:frontend": "node --env-file=.env ./node_modules/.bin/next dev frontend",
    "dev:workers": "npm run dev --workspace=backend",
    "test": "npm run test --workspaces --if-present"
  }
}
